Metadata-Version: 2.4
Name: tittle-classfication
Version: 4.0.0
Summary: 视频标题分类和重命名工具
Author: Starzzb
License-Expression: MIT
Project-URL: Homepage, https://github.com/Starzzb/tittle-classfication
Project-URL: Repository, https://github.com/Starzzb/tittle-classfication.git
Keywords: video,classification,rename,nlp,ollama,zhipu,vision,mimo
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: pandas>=2.0.0
Requires-Dist: jieba>=0.42.1
Requires-Dist: onnxruntime>=1.16.0
Requires-Dist: opencv-python>=4.8.0
Requires-Dist: numpy>=1.24.0
Requires-Dist: Pillow>=9.0.0
Requires-Dist: open-clip-torch>=2.20.0
Requires-Dist: torch>=2.0.0
Requires-Dist: torchvision>=0.15.0

# 视频/图片标题批量标准化重命名工具

## 目录

- [项目简介](#项目简介)
- [核心功能](#核心功能)
- [环境要求](#环境要求)
- [安装步骤](#安装步骤)
- [快速开始](#快速开始)
- [阶段一：扫描与生成](#阶段一扫描与生成)
- [阶段 1b：AI 优化标题（可选）](#阶段-1bai-优化标题可选)
- [阶段 1c：视觉理解提取关键词](#阶段-1c视觉理解提取关键词)
- [阶段二：核对与重命名](#阶段二核对与重命名)
- [参数详解](#参数详解)
- [高级功能](#高级功能)
- [常见问题](#常见问题)
- [更新日志](#更新日志)

---

## 项目简介

本工具是一套**三阶段媒体文件批量重命名系统**，专为本地视频/图片库管理设计。通过智能分词、AI 清洗和视觉识别，将杂乱的文件名转换为**标准化、可检索、语义清晰**的格式，同时保证操作的安全性与可逆性。

**适用场景**：
- 整理下载目录中的大量视频/图片文件
- 为媒体库建立统一命名规范
- 为后续 AI 分类/标签系统准备数据
- 批量清理历史遗留的混乱文件名

---

## 核心功能

| 功能模块 | 特性描述 |
|---------|---------|
| 智能扫描 | 递归遍历指定目录，支持 10+ 种视频格式和 7 种图片格式 |
| 语义分析 | 基于 Jieba 分词 + TF-IDF 算法，自动提取标题核心关键词 |
| 无意义检测 | 程序自动识别 hash、Telegram 来源、IMG_xxx 等无意义标题 |
| 自动跳过 | 已分类的文件（以 `[` 开头的标题）自动跳过，避免重复处理 |
| AI 优化 | 支持 Ollama、智谱、gcli 三种 API，批量精简标题 |
| 视觉识别 | 支持 MiMo、gcli 视觉模型，从视频/图片提取关键词 |
| 人体检测 | 使用 UHD 超轻量模型自动找到视频中包含人体的帧（默认启用） |
| 智能压缩 | 自动压缩图片/视频帧，避免 API 传输过大文件 |
| 水印优先 | 视觉识别优先提取水印、社交媒体名称、博主ID等关键信息 |
| 安全预览 | 三阶段设计，先生成待审表，人工确认后再执行，零风险操作 |
| 冲突避让 | 自动检测文件名冲突，智能追加序号（`_1`, `_2`），防止覆盖 |
| 增量模式 | 支持多次扫描追加记录，适合分批处理大型媒体库 |
| 目录排除 | 灵活排除特定目录（按名称/路径），避免扫描系统文件夹 |
| 详细日志 | 生成执行日志与错误报告，便于审计与问题排查 |

---

## 环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| Python | 3.10+ | 推荐 3.12 |
| 操作系统 | Windows / macOS / Linux | 全平台兼容 |
| 磁盘空间 | >= 100MB | 包含 onnxruntime 等依赖 |
| 权限要求 | 目标目录读写权限 | 必需 |
| ffmpeg | 全局 PATH | 用于阶段 1c 视频帧提取 |
| Ollama | （可选）本地部署 | 用于阶段 1b AI 优化标题 |

---

## 安装步骤

### 1. 安装 uv（Python 包管理器）

**Windows（PowerShell）**：
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**macOS / Linux**：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 2. 克隆或下载项目文件

将以下文件保存到同一目录：
- `stage1_extract_propose.py` — 阶段一：扫描与关键词提取
- `stage1b_ai_refine.py` — 阶段 1b（可选）：AI 优化标题
- `stage1c_vision_refine.py` — 阶段 1c：视觉理解提取关键词
- `stage1c_frame_selector.py` — 人体检测模块
- `stage2_apply_rename.py` — 阶段二：核对与重命名
- `renamecsv.py` — 辅助工具：批量修改 CSV 审核状态
- `pyproject.toml` — 项目配置与依赖
- `models/human_detection/` — UHD 人体检测模型目录

### 3. 创建虚拟环境并安装依赖

```powershell
cd your-project-path
uv venv --python 3.12
uv sync
```

### 4. 配置 AI API（可选）

在项目根目录创建 `.env` 文件：

```env
ZHIPU_API_KEY=your_zhipu_key_here
MIMO_API_KEY=your_mimo_key_here
GCLI_API_KEY=your_gcli_key_here
```

---

## 快速开始

```powershell
# 步骤1：扫描目录并生成待审表（自动标记 needs_vision）
uv run python stage1_extract_propose.py -d "F:\Videos"

# 步骤2：（可选）AI 优化标题
uv run python stage1b_ai_refine.py -p gcli

# 步骤3：视觉识别提取无意义标题的关键词（默认启用人体检测）
uv run python stage1c_vision_refine.py

# 步骤4：用编辑器打开 CSV，核对并修改
code title_review.csv

# 步骤5：模拟运行，预览结果
uv run python stage2_apply_rename.py --dry-run

# 步骤6：确认无误，执行重命名
uv run python stage2_apply_rename.py
```

---

## 阶段一：扫描与生成

### 功能说明

递归扫描指定目录中的所有视频和图片文件，提取文件名，通过分词算法生成标准化建议名称，输出为 CSV 待审表。

- **自动跳过已分类文件**：以 `[` 开头的文件名会被识别为已分类，自动跳过
- **关键词过滤**：提取的关键词自动去除 `# @ ！？` 等特殊符号，只保留中文和英文单词
- **标题预处理**：自动去除时间戳、@ 提及、hash 串、Telegram 等来源标识、技术参数
- **无意义检测**：自动识别 hash、Telegram 来源、IMG_xxx 等无意义标题，标记 `needs_vision=true`
- **支持图片格式**：`.jpg`, `.jpeg`, `.png`, `.bmp`, `.webp`, `.gif`, `.tiff`

### 基本用法

```powershell
# 扫描单个目录
uv run python stage1_extract_propose.py -d "F:\Download"

# 扫描并指定输出文件
uv run python stage1_extract_propose.py -d "F:\Download" -o "my_review.csv"

# 排除特定目录
uv run python stage1_extract_propose.py -d "F:\Download" --exclude-dir "temp"

# 排除多个目录
uv run python stage1_extract_propose.py -d "F:\Download" \
  --exclude-dir "temp" \
  --exclude-dir "cache" \
  --exclude-dir "F:\Download\broken"

# 追加模式（保留旧记录）
uv run python stage1_extract_propose.py -d "E:\Movies" -a
```

### 参数说明

| 参数 | 简写 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| `--target-dir` | `-d` | **必需** | - | 要扫描的目录路径 |
| `--output` | `-o` | 可选 | `title_review.csv` | 输出待审表文件名 |
| `--append` | `-a` | 可选 | `False` | 启用追加模式 |
| `--exclude-dir` | - | 可选（可多次） | - | 排除的目录名或绝对路径 |
| `--force-reclassify` | - | 可选 | `False` | 强制重分类：处理所有文件，有中括号的提取原始标题 |

### 强制重新分类

使用 `--force-reclassify` 可以：
1. **处理所有文件**（不跳过已有中括号的文件）
2. 如果有 `[xxx]_原始标题` 格式，提取后面的原始标题
3. 如果没有中括号，直接使用原标题进行处理
4. 输出新的 CSV 供审核

```powershell
# 强制重新分类所有文件
uv run python stage1_extract_propose.py -d "F:\Videos" --force-reclassify -o "review_reclassify.csv"
```

### 输出文件结构

生成的 `title_review.csv` 包含以下列：

| 列名 | 说明 | 示例 |
|------|------|------|
| `original_path` | 文件绝对路径 | `F:\Videos\abc.mp4` |
| `original_title` | 提取后的原始标题 | `Python入门教程` |
| `original_filename` | 原始文件名（含中括号） | `[Python_教程]_Python入门教程` |
| `proposed_title` | 算法生成的建议名称 | `[Python_入门_教程]_Python入门教程` |
| `review_status` | 审核状态（默认"待审核"） | `待审核` |
| `final_name` | 最终采用的名称（默认同 proposed_title） | `[Python_入门_教程]_Python入门教程` |
| `needs_vision` | 是否需要视觉识别（程序判定） | `false` |

### 无意义标题判定规则

程序自动检测以下模式，标记 `needs_vision=true`：

| 规则 | 示例 |
|------|------|
| IMG/VID/DCIM 前缀 | `IMG_7940`、`VID_20240115` |
| Telegram/TG 且无中文 | `Telegram@ciyuanb@-merged-...` |
| `v(数字)-Telegram-@` | `v (6)-Telegram-@ciyuanbb-merged-...` |
| @群组名无实质内容 | `@user1@user2-12345` |
| 纯 hex/hash | `25bdc148`、`6330BC93-0DF7-...` |
| 纯数字/日期 | `20240115`、`25-05-08` |
| 中文日期格式 | `4月23日 (7)`、`12月1日` |
| merged-时间戳后缀 | `xxx-merged-1778832994522` |
| video-output 前缀 | `video-output-6330BC93-...` |
| 长字母数字混合 | `VWhcUHkhQh`、`tEeFNclpVBPc` |
| 下划线开头 | `_V (1).mp4` |

---

## 阶段 1b：AI 优化标题（可选）

### 功能说明

读取阶段一生成的 CSV，调用 AI 大语言模型对标题进行智能精简，去除冗余信息（时间戳、群组名、hash、技术参数等），只保留核心标题。

- `needs_vision=false` 的标题：AI 精简后写入 `final_name`
- `needs_vision=true` 的标题：保留 stage1 的 `proposed_title`，留给阶段 1c 处理

### 支持的 Provider

| Provider | 默认模型 | 环境变量 | 特点 |
|----------|---------|---------|------|
| `ollama` | qwen2.5:7b-instruct-q4_K_M | - | 本地运行，免费 |
| `zhipu` | GLM-4.7-Flash | ZHIPU_API_KEY | 云端，中文优秀 |
| `gcli` | gemini-3-flash-preview | GCLI_API_KEY | 云端，免费 |

### 基本用法

```powershell
# 使用 Ollama（本地）
uv run python stage1b_ai_refine.py -p ollama

# 使用智谱 API
uv run python stage1b_ai_refine.py -p zhipu

# 使用 gcli API
uv run python stage1b_ai_refine.py -p gcli

# 指定模型
uv run python stage1b_ai_refine.py -p gcli -m "其他模型名"
```

---

## 阶段 1c：视觉理解提取关键词

### 功能说明

对于 `needs_vision=true` 的文件（hash、纯数字、IMG_xxx 等），使用视觉大模型从视频/图片中提取内容关键词。

**核心特性**：
- **人体检测**（默认启用）：使用 UHD 超轻量模型自动找到视频中包含人体的帧
- **智能压缩**：自动压缩图片/视频帧，避免 API 传输过大文件
- **水印优先**：优先提取水印、社交媒体名称、博主ID等关键信息
- **图片支持**：图片文件直接压缩后送入视觉模型，跳过人体检测

### 工作流程

```
视频文件 → 人体检测找到最佳帧 → 压缩 → 视觉模型分析 → 提取关键词
图片文件 → 直接压缩 → 视觉模型分析 → 提取关键词
```

### 支持的 Provider

| Provider | 默认模型 | 环境变量 | 特点 |
|----------|---------|---------|------|
| `gcli`（默认） | gemini-3-flash-preview | GCLI_API_KEY | 云端，免费 |
| `mimo` | mimo-v2-omni | MIMO_API_KEY | 小米自研 |

### 基本用法

```powershell
# 默认使用 gcli（人体检测自动启用）
uv run python stage1c_vision_refine.py

# 使用 MiMo 模型
uv run python stage1c_vision_refine.py -p mimo

# 禁用人体检测（使用固定时间点提取帧）
uv run python stage1c_vision_refine.py --no-frame-selector

# 重试之前失败的行（vision_description 为 [ERROR] 的）
uv run python stage1c_vision_refine.py --retry-errors

# 仅处理单个文件
uv run python stage1c_vision_refine.py --single "IMG_7940.MP4"

# 处理所有标题（忽略 needs_vision 标记）
uv run python stage1c_vision_refine.py --all

# 指定截取时间点（禁用人体检测时生效）
uv run python stage1c_vision_refine.py --no-frame-selector --timestamp "00:01:30"

# 调整图片压缩大小
uv run python stage1c_vision_refine.py --max-image-size 640

# 模拟运行
uv run python stage1c_vision_refine.py --dry-run
```

### 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--csv` | 可选 | `title_review.csv` | 待审表路径 |
| `--output` | 可选 | 覆盖原文件 | 输出路径 |
| `--provider` | 可选 | `gcli` | API 提供商 |
| `--model` | 可选 | 自动匹配 | 模型名称 |
| `--api-key` | 可选 | - | API key（也可用 `.env`） |
| `--no-frame-selector` | 可选 | `False` | 禁用人体检测预处理 |
| `--model-path` | 可选 | `models/human_detection/...` | UHD 模型路径 |
| `--step-seconds` | 可选 | `5.0` | 人体检测帧提取步长（自适应调整） |
| `--max-retries` | 可选 | `3` | 人体检测最大重试次数 |
| `--conf-threshold` | 可选 | `0.5` | 人体检测置信度阈值 |
| `--max-image-size` | 可选 | `800` | 图片最大边长（像素） |
| `--timestamp` | 可选 | 自动 | 固定截取时间点（默认使用视频 1/4 位置） |
| `--batch-size` | 可选 | `3` | 每批处理数量 |
| `--delay` | 可选 | `2.0` | 批次间隔秒数 |
| `--retries` | 可选 | `3` | API 失败重试次数 |
| `--retry-errors` | 可选 | `False` | 重新处理之前失败的行 |
| `--single` | 可选 | - | 仅处理指定文件名 |
| `--all` | 可选 | `False` | 处理所有标题 |
| `--dry-run` | 可选 | `False` | 模拟运行 |

### 自适应时间策略

系统会根据视频时长自动调整时间策略，无需手动设置：

**默认帧（无人体检测命中时）**：
- 自动使用视频 **1/4 位置**（如 60s 视频 → 15s 处）

**人体检测步长**：
- 自动覆盖视频前 **1/3**，步长范围 2-10s
- 10s 视频 → 步长 2s（检测 0s, 2s, 4s）
- 60s 视频 → 步长 6.7s（检测 0s, 6.7s, 13.4s）
- 3分钟视频 → 步长 10s（检测 0s, 10s, 20s）

**安全时间戳**：
- 如果目标时间超过视频时长，自动调整到安全位置
- 距离视频末尾保留 2s 安全距离

### 输出列

| 列名 | 说明 | 示例 |
|------|------|------|
| `vision_description` | 文件内容描述 | 一个年轻女性在室内跳舞 |
| `vision_keywords` | 提取的关键词 | 舞蹈, 室内, 年轻女性 |
| `human_detected` | 是否检测到人体 | true / false / N/A (图片) |
| `detection_confidence` | 人体检测置信度 | 0.78 |
| `detection_timestamp` | 检测到人体的时间点 | 2.0s |

### 关键词提取优先级

视觉模型提取关键词时遵循以下优先级规则：

1. **最高优先级**：水印上的博主名称（个人昵称/艺名）、出品平台（Telegram、Twitter、TikTok等）
2. **第二优先级**：穿着、动作、行为、场景环境

注意：@开头的频道名/群组名（如 @ciyuanbb）不会被提取，只提取可能的个人博主昵称。

### 工作流程

```
视频文件 → 获取时长 → 人体检测（自适应步长）→ 找到最佳帧 → 压缩 → 视觉模型 → 关键词
图片文件 → 压缩 → 视觉模型 → 关键词
```

---

## 阶段二：核对与重命名

### 功能说明

读取 CSV 待审表，仅对标记为"已确认"的记录执行安全重命名，自动处理冲突与异常。

### 基本用法

```powershell
# 模拟运行（推荐首次执行）
uv run python stage2_apply_rename.py --dry-run

# 正式执行
uv run python stage2_apply_rename.py

# 指定自定义 CSV 路径
uv run python stage2_apply_rename.py -c "E:\work\review.csv"
```

### 人工核对流程

1. **打开 CSV 文件**
   ```powershell
   code title_review.csv
   ```

2. **编辑审核状态**
   - 将需要重命名的行，`review_status` 列改为 **`已确认`**
   - 保持不需要的行为"待审核"或直接删除该行
   - 可修改 `final_name` 列自定义最终名称

3. **保存文件**
   - 编码必须为 UTF-8 with BOM（Excel 兼容性）
   - 保持 CSV 格式，不要修改列名

4. **模拟验证**
   ```powershell
   uv run python stage2_apply_rename.py --dry-run
   ```

5. **执行重命名**
   ```powershell
   uv run python stage2_apply_rename.py
   ```

---

## 参数详解

### 阶段一完整参数表

```powershell
uv run python stage1_extract_propose.py \
  -d "F:\Videos" \                    # 必需：扫描目录
  -o "review.csv" \                   # 可选：输出文件名
  -a \                                # 可选：追加模式
  --exclude-dir "temp" \              # 可选：排除目录名
  --exclude-dir "cache" \             # 可选：可多次使用
  --exclude-dir "F:\Videos\broken"    # 可选：排除绝对路径
```

### 阶段 1b 完整参数表

```powershell
uv run python stage1b_ai_refine.py \
  -c "review.csv" \                   # 可选：CSV 路径
  -o "output.csv" \                   # 可选：输出路径
  -p gcli \                           # 可选：ollama/zhipu/gcli
  -m "gemini-3-flash-preview" \       # 可选：模型名称
  --api-key "your_key" \              # 可选：API key
  --column "gcli_title" \             # 可选：新列名
  --batch-size 5 \                    # 可选：每批标题数
  --delay 2 \                         # 可选：批次间隔秒数
  --dry-run                           # 可选：模拟模式
```

### 阶段 1c 完整参数表

```powershell
uv run python stage1c_vision_refine.py \
  -c "review.csv" \                   # 可选：CSV 路径
  -o "output.csv" \                   # 可选：输出路径
  -p gcli \                           # 可选：mimo/gcli
  -m "gemini-3-flash-preview" \       # 可选：模型名称
  --api-key "your_key" \              # 可选：API key
  --no-frame-selector \               # 可选：禁用人体检测
  --step-seconds 3 \                  # 可选：人体检测步长
  --max-retries 5 \                   # 可选：人体检测重试次数
  --conf-threshold 0.6 \              # 可选：人体检测置信度
  --max-image-size 640 \              # 可选：图片压缩大小
  --timestamp "00:00:05" \            # 可选：固定截取时间点
  --batch-size 3 \                    # 可选：每批处理数
  --delay 2 \                         # 可选：批次间隔秒数
  --retries 3 \                       # 可选：失败重试次数
  --retry-errors \                    # 可选：重试之前失败的行
  --single "IMG_7940.MP4" \           # 可选：仅处理单个文件
  --all \                             # 可选：处理所有标题
  --dry-run                           # 可选：模拟模式
```

### 阶段二完整参数表

```powershell
uv run python stage2_apply_rename.py \
  -c "review.csv" \                   # 可选：CSV 路径
  --dry-run                           # 可选：模拟模式
```

---

## 高级功能

### 1. 自定义关键词数量

编辑 `stage1_extract_propose.py`，修改顶部配置：
```python
TOP_KEYWORDS = 5  # 默认 5，改为其他值可调整提取数量
```

### 2. 自定义停用词表

在 `load_stopwords()` 函数中添加自定义词汇：
```python
def load_stopwords() -> set:
    return {
        "的", "了", "在",
        "你的词1",  # 添加此行
        "你的词2",  # 添加此行
    }
```

### 3. 批量处理多个目录

创建批处理脚本 `batch_scan.bat`（Windows）：
```batch
@echo off
set SCRIPT=stage1_extract_propose.py

uv run python %SCRIPT% -d "F:\Videos" -o "review.csv"
uv run python %SCRIPT% -d "G:\Movies" -a -o "review.csv"
uv run python %SCRIPT% -d "H:\Downloads" -a -o "review.csv"

echo 所有目录扫描完成！
pause
```

### 4. 仅处理特定格式

编辑 `VIDEO_EXTENSIONS` 和 `IMAGE_EXTENSIONS` 集合：
```python
VIDEO_EXTENSIONS = {'.mp4', '.mkv'}  # 仅处理 MP4 和 MKV
IMAGE_EXTENSIONS = {'.jpg', '.png'}   # 仅处理 JPG 和 PNG
```

### 5. 快速批量确认所有记录

使用 `renamecsv.py` 将 CSV 中所有记录的 `review_status` 批量改为"已确认"：
```powershell
uv run python renamecsv.py
```

---

## 常见问题

### Q1：提示"文件或目录损坏且无法读取"

**原因**：目标目录包含损坏的文件夹或无权限访问的系统目录。

**解决**：
```powershell
uv run python stage1_extract_propose.py -d "F:\" --exclude-dir "F:\Download\love"
```

### Q2：CSV 在 Excel 中打开乱码

**原因**：编码不是 UTF-8 with BOM。

**解决**：
1. 用 VS Code 打开 CSV
2. 右下角点击编码 -> "通过编码保存"
3. 选择 "UTF-8 with BOM"
4. 重新用 Excel 打开

### Q3：重命名后文件"消失"了

**原因**：文件仍在原目录，只是名称变了。

**解决**：
- 在资源管理器中按"修改时间"排序
- 或搜索新名称中的关键词

### Q4：提示"权限不足"或"文件被占用"

**解决**：
1. 关闭所有播放器
2. 关闭资源管理器预览窗格
3. 以管理员身份运行终端
4. 重启电脑后重试

### Q5：如何撤销重命名？

**方法 1**：从 CSV 的 `original_title` 列手动还原

**方法 2**：执行前备份目录
```powershell
robocopy "F:\Videos" "F:\Videos_Backup" /E /NFL /NDL /NJH /NJS
```

### Q6：API 调用失败/超时

**解决**：
```powershell
# 增加重试次数
uv run python stage1c_vision_refine.py --retries 5

# 重试失败的行
uv run python stage1c_vision_refine.py --retry-errors
```

### Q7：单个文件处理失败

**解决**：
```powershell
# 单独处理指定文件
uv run python stage1c_vision_refine.py --single "文件名"

# 换个模型重试
uv run python stage1c_vision_refine.py --single "文件名" -p mimo
```

### Q8：扫描速度太慢

**优化方案**：
1. 排除无关目录（`--exclude-dir`）
2. 仅扫描特定子目录，而非整个磁盘
3. 关闭杀毒软件实时防护（临时）
4. 使用 SSD 而非机械硬盘

### Q9：人体检测不准确

**解决**：
```powershell
# 降低置信度阈值（检测更敏感）
uv run python stage1c_vision_refine.py --conf-threshold 0.3

# 增加重试次数和步长
uv run python stage1c_vision_refine.py --max-retries 5 --step-seconds 3

# 完全禁用人体检测
uv run python stage1c_vision_refine.py --no-frame-selector
```

### Q10：图片处理太慢

**解决**：
```powershell
# 减小压缩尺寸
uv run python stage1c_vision_refine.py --max-image-size 640
```

---

## 更新日志

### v4.0.0（当前版本）
- **重大更新**：支持图片文件（.jpg, .jpeg, .png, .bmp, .webp, .gif, .tiff）
- **重大更新**：人体检测预处理默认启用（UHD 超轻量模型）
- 新增智能压缩：自动压缩图片/视频帧，避免 API 传输过大文件
- 新增水印优先：视觉模型优先提取水印、社交媒体名称、博主ID
- 图片文件跳过人体检测，直接压缩后送入视觉模型
- 新增 `--no-frame-selector` 参数禁用人体检测
- 新增 `--max-image-size` 参数控制压缩大小
- 新增 `human_detected`、`detection_confidence`、`detection_timestamp` 输出列
- 无意义标题判定增强：中文日期、长字母数字混合、下划线开头等
- `[未分类]` 标记不再跳过，会重新处理

### v3.0.0
- 新增阶段 1c：视觉理解提取关键词（支持 MiMo、gcli）
- 新增 `--retry-errors` 重试失败的行
- 新增 `--single` 处理单个视频
- 新增 gcli API 支持（OpenAI 兼容，gemini-3-flash-preview）
- 无意义标题判定迁移到 stage1（`needs_vision` 列）
- stage1b 简化为读取 `needs_vision` 标记
- 视觉提示词优化，避免 AI 拒绝回复

### v2.0.0
- 新增阶段 1b：AI 优化标题（支持 Ollama、智谱 API）
- 新增自动跳过已分类文件（以 `[` 开头的标题）
- 新增关键词符号过滤，只保留中文和英文单词
- 新增标题预处理（时间戳、hash、来源标识、技术参数过滤）
- 移除 transformers/torch 依赖，项目更轻量
- 新增 `.env` 配置文件支持

### v1.0.0
- 双阶段安全重命名
- Jieba 分词 + TF-IDF 关键词提取
- 目录排除功能
- 增量追加模式
- 冲突自动避让
- 详细日志系统

---

**祝您使用愉快！如有建议或需求，欢迎反馈。**
